 "use client";import {supabaseBrowser} from "@/lib/supabase-browser";export function AdminActions({id}:{id:number}){async function act(status:string){const s=supabaseBrowser();await s.from("mods").update({status}).eq("id",id);location.reload()}return <div style={{display:"flex",gap:6}}><button className="btn success" onClick={()=>act("approved")}>Approve</button><button className="btn danger" onClick={()=>act("rejected")}>Reject</button><button className="btn" onClick={()=>act("hidden")}>Hide</button></div>}
