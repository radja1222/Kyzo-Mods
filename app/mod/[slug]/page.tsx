import {supabaseServer} from "@/lib/supabase-server";import {notFound} from "next/navigation";import {FavoriteButton} from "@/components/FavoriteButton";import {CommentForm} from "@/components/CommentForm";
export default async function Mod({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=await supabaseServer();const {data:m}=await s.from("mods").select("*,profiles(id,username,avatar_url,bio),categories(name,game)").eq("slug",slug).eq("status","approved").single();if(!m)notFound();const {data:comments}=await s.from("comments").select("id,content,created_at,profiles(username,avatar_url)").eq("mod_id",m.id).eq("status","visible").order("created_at",{ascending:false});return <div className="container detail"><main className="detailMain">{m.thumbnail_url&&<img className="detailImg" src={m.thumbnail_url}/>}<span className="tag">{m.categories.game.toUpperCase()} · {m.categories.name}</span><h1>{m.title}</h1><p>{m.description}</p><div className="actions"><FavoriteButton modId={m.id}/>{m.mod_type==="free"?<a className="btn primary" href={`/api/download/${m.id}`}>Download FREE</a>:<a className="btn primary" href={`/checkout/${m.id}`}>Buy · Rp {Number(m.price).toLocaleString("id-ID")}</a>}</div><h2 style={{marginTop:35}}>Reviews & Comments</h2><CommentForm modId={m.id}/>{(comments||[]).map((c:any)=><div className="comment" key={c.id}><b>{c.profiles?.username}</b><br/><span>{c.content}</span><br/><small>{new Date(c.created_at).toLocaleString("id-ID")}</small></div>)}</main><aside className="side"><div className="sideRow"><span>Creator</span><b><a href={`/creator/${m.profiles.username}`}>{m.profiles.username}</a></b></div><div className="sideRow"><span>Type</span><b>{m.mod_type.toUpperCase()}</b></div><div className="sideRow"><span>Version</span><b>{m.version}</b></div><div className="sideRow"><span>Downloads</span><b>{m.downloads}</b></div><div className="sideRow"><span>Views</span><b>{m.views}</b></div></aside></div>}
                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    
