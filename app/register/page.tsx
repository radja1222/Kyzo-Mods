 "use client";
import {useState} from "react"; import {supabaseBrowser} from "@/lib/supabase-browser";
export default function Register(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [username,setUsername]=useState("");const [msg,setMsg]=useState("");
async function go(e:any){e.preventDefault();const s=supabaseBrowser();const {data,error}=await s.auth.signUp({email,password,options:{data:{username}}});if(error){setMsg(error.message);return}setMsg(data.session?"Akun dibuat.":"Cek email untuk verifikasi akun.");}
return <div className="form"><div className="formCard"><h1>Create Account</h1><p className="muted">Buat akun creator KyzoMods.</p>{msg&&<div className="alert">{msg}</div>}<form onSubmit={go}><div className="field"><label>Username</label><input value={username} onChange={e=>setUsername(e.target.value)} minLength={3} maxLength={32} required/></div><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required/></div><button className="btn primary" style={{width:"100%"}}>Register</button></form><p className="muted">Sudah punya akun? <a style={{color:"var(--orange2)"}} href="/login">Login</a></p></div></div>}
  
